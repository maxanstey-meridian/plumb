public sealed class CheckoutTests
{
    // Received.InOrder(() => gateway.Charge());
    private const string Example = "new MockSequence() and VerifyInOrder()";
    public void Saves_order() => Assert.True(repository.Contains(order));
}
